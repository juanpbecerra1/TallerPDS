spring:
  application:
    name: service-config
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: file:///D:/config-data
  security:
    user:
      name: admin
      password: admin
server:
  port: 9004
